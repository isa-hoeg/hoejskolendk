---
// Henter parametrene fra Astro.props
const { htmlheading="h2", size="medium", font="sans", stylingclasses=""} = Astro.props; 
const Htmlheading = htmlheading;
 
//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige størrelser.
const sizeClasses = {
"h1_hero": "text-[160px] text-[var(--color-text)]",
"h1_single": "text-[140px] text-[var(--color-text)]",
"h2_light": "text-[90px] text-[var(--color-secondary)]",
"h2_dark": "text-[90px] text-[var(--color-text)] text-base/10",
"h3": "text-[80px] text-[var(--color-text)] text-base/10",
"h4_dark": "text-[40px] text-[var(--color-text)] text-base/10",
"h4_light": "text-[40px] text-[var(--color-secondary)] text-base/10",
};

//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige skrifttyper.
const fontClasses = {
"overskrifter": "font-display",
"text": "font-serif",
};
---
<Htmlheading class:list={[stylingclasses, sizeClasses[size], fontClasses[font]]}><slot /></Htmlheading>